def is_divisible_without_remainder(a, b):
    # Your code here
    # Check if a is divisible by b without a remainder
    # using only arithmetic operations
    # Return 'YES' if divisible, 'NO' otherwise

    return result

# Test the function with an example input
if __name__ == "__main__":
    a = int(input("Enter a number (a): "))
    b = int(input("Enter another number (b): "))
    print(is_divisible_without_remainder(a, b))
